fastapi==0.116.1
uvicorn[standard]==0.35.0
pydantic==2.13.4
# MiniMax-H3 landed after Diffusers 0.39.0. Pin the exact upstream source
# commit that contains MiniMaxH3ModularPipeline; never install an unpinned main.
diffusers @ https://github.com/huggingface/diffusers/archive/3a2f35d4efa4c059c8bfb3bc0d6c906264895c81.zip
transformers==5.14.1
accelerate==1.14.0
peft==0.19.1
huggingface-hub==1.24.0
safetensors==0.8.0
Pillow==11.3.0
numpy==2.3.2
psutil==7.0.0
# Official H3 consumer-GPU recipe uses TorchAO INT8 weight-only.
torchao==0.17.0
# H3 Ref2VA video/audio .from_file decoders use PyAV.
av==18.0.0
# Compatibility pack Diffusers vérifié dans l'image finale. Les dépendances
# plus spécialisées restent installables via l'allowlist isolée du Worker.
bitsandbytes==0.49.2
sentencepiece==0.2.2
einops==0.8.2
ftfy==6.3.1
imageio==2.37.4
imageio-ffmpeg==0.6.0
